{
  "dependencies": {
    "dotenv": "^16.0.3",
    "fs-extra": "^11.1.1",
    "prettier": "^2.8.8",
    "prettier-plugin-solidity": "^1.1.3",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --basepath . -o . SimpleStorage.sol"
  },
  "devDependencies": {
    "ethers": "^5.0.0"
  }
}
